export interface tileItem {
    position: [number, number];
    id: number;
    name: string;
    tags: string[];
    snap: string;
    src: string;
    scale: number;
    constScale: number;
    orientation: number;
    counter: number;
}
